# Sandbox moved-main verification

This sandbox-only commit advances main after release preparation PR #1 merged
at `99cd93a51e5f46c8ee88d8c00cbc853afd2312b0`. Resuming signing by PR number must
still sign that reviewed merge commit, after owner approval, even though main
now contains this later commit. Sandbox artifacts are for testing only.
